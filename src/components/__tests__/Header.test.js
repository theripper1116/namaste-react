// import Header from "../Header";
// import { render, waitFor, screen } from "react-testing-library";

// it("Should check if the Header component has button", async () => {
//   render(<Header />);
//   await waitFor(() => screen.getByRole("button"));
// });
