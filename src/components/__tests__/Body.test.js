// import { render, screen } from "@testing-library/react";
// import Body from "../Body";

// it("Should load Body Component with text Loading ...", () => {
//   render(<Body />);
//   const loadingText = screen.getByText("Loading....");
//   expect(loadingText).toBeInTheDocument();
// });
